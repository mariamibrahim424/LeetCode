/**
 * @param {number} num1
 * @param {number} num2
 * @return {number}
 */
var sum = function(num1, num2) {
    if(Number.isInteger(num1) && Number.isInteger(num2)){
        return num1+num2;
    }
};